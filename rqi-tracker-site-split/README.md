# RQI Tracker (split site)

This folder is a split version of `rqi-tracker.html`:

- `index.html`
- `assets/styles.css`
- `assets/app.js`

## Run locally

From repo root:

```bash
python3 -m http.server 5174
```

Open `http://localhost:5174/rqi-tracker-site-split/`.

